import React from 'react';
import { TouchableOpacity, Text } from 'react-native';

export const AddList = () => { return (
    <TouchableOpacity>
      <Text> Hello </Text>
    </TouchableOpacity>
  );
};